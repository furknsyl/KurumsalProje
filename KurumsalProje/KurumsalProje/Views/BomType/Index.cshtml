@using KurumsalProje.Models
@model List<BSMGRBCEBOM001>
@{
    ViewBag.Title = "Index";
}

<h2>Ürün Ağacı Bilgileri</h2>
<br />

<table class="table table-bordered">

    <tr>
        <th>ID</th>
        <th>Ürün Ağacı Tipi</th>
        <th>Ürün Ağacı Tipi Açıklaması</th>
        <th>Pasif mi ?</th>
        <th>Sil</th>
        <th>Güncelle</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>@item.COMCODE</td>
            <td>@item.DOCTYPE</td>
            <td>@item.DOCTYPETEXT</td>
            <td>@(item.ISPASSIVE == 1 ? "Evet" : "Hayır")</td>
            <td><a href="/BomType/KayıtSil/@item.COMCODE" class="btn btn-danger">Sil</a></td>
            <td><a href="/BomType/KayıtGuncelle/@item.COMCODE" class="btn btn-success">Güncelle</a></td>


        </tr>

    }
</table>

<a href="/BomType/YeniKayıt" class="btn btn-primary">Yeni Ürün Ağacı Ekle</a>
